<%= form_for(@come_in_type) do |f| %>
  <% if @come_in_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@come_in_type.errors.count, "error") %> prohibited this come_in_type from being saved:</h2>

      <ul>
      <% @come_in_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
